import './index.css';

import { AllPageWrapper } from '@shared/ui/components';

import { Providers } from './providers';

export const App = () => {
  return (
    <AllPageWrapper>
      <Providers />
    </AllPageWrapper>
  );
};

export default App;
